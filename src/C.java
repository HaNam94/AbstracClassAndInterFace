public final class C { // sử dụng với lớp thì không thể cho phép lớp khác kế thừa
    // sử dụng với pt thì không thể ghi đè
    // biến thì không thể gán lại
    // không thể sử dụng tring final và abstract
    final int go(){
        return 1;
    }
}
